<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <title>DackSite</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.6.1/css/font-awesome.min.css">
  </head>

  <body>
    <div class="navbar navbar-expand-lg navbar-light bg-light">
      <div class="container">

        <%= link_to DackSite, root_path, class: 'navbar-brand' %>

        <div class="navbar navbar-responsive">
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="buffer-left-right color-text "><%=link_to "Загрузить фотку", new_photo_path %></li> |
              <li class="buffer-left-right color-text "><%=link_to "Профиль", current_user %></li> |
              <li class="buffer-left-right"><%= link_to "Выйти", destroy_user_session_path, :method => :delete %></li>
            <% else %>
              <li class="buffer-left-right"><%=link_to "Для админа", new_user_session_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>

    <% flash.each do |key, value| %>
      <div class="<%= flash_class(key) %>" role="alert">
        <%= value %>
        <button type="button" class="close" data-dismiss="alert" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
    <% end %>

    <%= yield %>

    <footer>
      <p class="text-center buffer-top">&copy; Dack9  <%= Time.now.year %></p>
    </footer>
  </body>
</html>
