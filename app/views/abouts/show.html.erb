<%= breadcrumbs [t('views.home.announces.header'), root_path], [t('views.abouts.show.header'), nil] %>

<div class="container main_bg_color rounded_corners">
  <div class="row mt-3">
    <%= raw(@about.main_text) %><br>
  </div>

  <% if current_user %>
    <%= link_to t('views.abouts.index.edit'), edit_about_path(@about), class: 'btn btn-sm btn-primary my-2 ml-md-5'%>
  <% end %>
</div>
