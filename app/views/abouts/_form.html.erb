<%= form_with(model: about, local: true) do |form| %>
  <% if about.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this about from being saved:</h2>

      <ul>
        <% about.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group mt-3">
    <%= form.label t('views.abouts.form.main_text') %>
    <%= form.text_area :main_text_ru, class: 'form-control my_height' %>
  </div>

  <div class="form-group mt-3">
    <%= form.label t('views.abouts.form.main_text') %>
    <%= form.text_area :main_text_en, class: 'form-control my_height' %>
  </div>

  <div class="actions">
    <%= form.submit t('views.abouts.form.save'), class: 'btn btn-success mb-2' %>
    <%= link_to t('views.abouts.edit.back'), about_path(about.id), class: 'btn btn-secondary mb-2' %>
  </div>
<% end %>
